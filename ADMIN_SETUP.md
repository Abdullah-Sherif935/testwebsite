# خطوات إعداد لوحة التحكم (Admin Panel Setup)

## المشكلة التي تم حلها:
1. **فصل جلسات الموقع الرئيسي عن لوحة التحكم**: الآن تسجيل الدخول/الخروج في الموقع الرئيسي لن يؤثر على لوحة التحكم والعكس صحيح.
2. **تقييد الوصول للوحة التحكم**: فقط الإيميل المحدد مسبقاً يمكنه الوصول للوحة التحكم.

## خطوات مهمة جداً:

### 1. إضافة متغير البيئة (Environment Variable)

افتح ملف `.env` في المشروع وأضف السطر التالي:

```env
VITE_ADMIN_EMAIL=your_admin_email@example.com
```

**⚠️ مهم جداً**: استبدل `your_admin_email@example.com` بالإيميل الفعلي الذي تريد استخدامه للوصول إلى لوحة التحكم.

مثال:
```env
VITE_ADMIN_EMAIL=abdullah.sherif@example.com
```

### 2. إعادة تشغيل السيرفر

بعد إضافة المتغير، **يجب** إعادة تشغيل سيرفر التطوير:

```bash
# أوقف السيرفر الحالي (Ctrl+C)
# ثم أعد تشغيله
npm run dev
```

## كيف يعمل النظام الجديد؟

### للموقع الرئيسي (Public Site):
- أي شخص يمكنه التسجيل وتسجيل الدخول عادياً
- الجلسة مستقلة تماماً عن لوحة التحكم
- تسجيل الخروج من الموقع لن يؤثر على لوحة التحكم

### للوحة التحكم (Admin Panel):
- فقط الإيميل المحدد في `VITE_ADMIN_EMAIL` يمكنه الوصول
- إذا سجل شخص آخر دخوله، سيحصل على رسالة "Access Denied"
- الجلسة مستقلة تماماً عن الموقع الرئيسي
- تسجيل الخروج من لوحة التحكم لن يؤثر على الموقع

## الملفات التي تم تعديلها:

1. **`src/context/AdminAuthContext.tsx`** (جديد)
   - Context منفصل للمصادقة في لوحة التحكم
   - يتحقق من أن الإيميل يطابق `VITE_ADMIN_EMAIL`

2. **`src/components/admin/ProtectedRoute.tsx`**
   - الآن يستخدم `AdminAuthContext` بدلاً من `AuthContext`
   - يعرض رسالة "Access Denied" للمستخدمين غير المصرح لهم

3. **`src/App.tsx`**
   - لوحة التحكم الآن محاطة بـ `AdminAuthProvider`
   - الموقع الرئيسي يستخدم `AuthProvider` العادي

4. **`.env.example`** (جديد)
   - نموذج لملف البيئة

## اختبار النظام:

### اختبار 1: تسجيل دخول عادي (موقع رئيسي)
1. اذهب إلى `/auth`
2. سجل دخول بأي حساب
3. ستكون مسجل دخول في الموقع الرئيسي فقط

### اختبار 2: محاولة الوصول للوحة التحكم بحساب عادي
1. سجل دخول بإيميل مختلف عن `VITE_ADMIN_EMAIL`
2. حاول الذهاب إلى `/admin`
3. يجب أن ترى رسالة "Access Denied"

### اختبار 3: تسجيل دخول Admin
1. اذهب إلى `/admin/login`
2. سجل دخول بالإيميل المحدد في `VITE_ADMIN_EMAIL`
3. يجب أن تدخل لوحة التحكم بنجاح

### اختبار 4: استقلالية الجلسات
1. سجل دخول للموقع الرئيسي
2. في تاب آخر، سجل دخول للوحة التحكم
3. سجل خروج من أحدهما
4. الآخر يجب أن يبقى مسجل دخول

## ملاحظات مهمة:

⚠️ **لا تنسى**:
- إضافة `VITE_ADMIN_EMAIL` إلى ملف `.env` في الـ Production (Vercel)
- إعادة تشغيل السيرفر بعد تغيير ملف `.env`
- استخدام إيميل حقيقي تملكه في `VITE_ADMIN_EMAIL`

✅ **الأمان**:
- لا تشارك قيمة `VITE_ADMIN_EMAIL` مع أحد
- لا ترفع ملف `.env` إلى GitHub (موجود في `.gitignore`)
- استخدم كلمة مرور قوية للحساب الإداري
