# دليل هيكل المشروع والملفات
هذا الملف يعتبر مرجعاً شاملاً للمطورين (أو لك شخصياً) لفهم مكونات المشروع، التقنيات المستخدمة، ووظيفة كل ملف ومجلد، مع دليل عملي لكيفية إجراء التعديلات.

---

## 1. اللغات والتقنيات المستخدمة
المشروع عبارة عن تطبيق ويب حديث (Modern Web App) مبني المتطلبات التالية:

| اللغة / الامتداد | الاسم | الوظيفة | مثال على الاستخدام |
| :--- | :--- | :--- | :--- |
| **`.tsx`** | TypeScript JSX | هي اللغة الأساسية للمشروع. تجمع بين منطق JavaScript وكود HTML لبناء واجهات المستخدم (Components). | كل ملفات الصفحات في `src/pages` والمكونات في `src/components`. |
| **`.ts`** | TypeScript | تستخدم لكتابة المنطق البرمجي (Logic) فقط، تعريف الأنواع (Types)، والوظائف المساعدة (Utils) بدون واجهة رسومية. | ملفات `src/hooks`, `src/services`, `src/types`. |
| **`.css`** | CSS (Tailwind) | تنسيق الموقع. نستخدم **Tailwind CSS v4** لكتابة التنسيقات إما داخل ملف الـ CSS مباشرة أو كـ classes داخل الـ TSX. | ملف `src/index.css` هو الملف الرئيسي للتنسيق. |
| **`.json`** | JSON | ملفات تخزين البيانات والإعدادات. تستخدم لتعريف مكتبات المشروع وللترجمة (عربي/إنجليزي). | ملف `package.json` (للمكتبات) وملفات الترجمة في `public/locales`. |
| **`.sql`** | SQL | لغة قواعد البيانات. تستخدم لإنشاء جداول وتعديل هيكل قاعدة البيانات في Supabase. | الملفات داخل مجلد `supabase/migrations`. |

---

## 2. تفصيل هيكل الملفات (أين أجد الكود؟)

### أ) الملفات الجذرية (Root Files)
تتحكم في إعدادات المشروع ككل.

*   **`package.json`**: بطاقة هوية المشروع. يحتوي على قائمة المكتبات المثبتة (`dependencies`) وأوامر التشغيل (`scripts` مثل `npm run dev`).
*   **`vite.config.ts`**: إعدادات "المحرك" (Vite). يخبر الجهاز كيف يقوم بتجميع الملفات وتشغيل السيرفر المحلي.
*   **`.env`**: (قد يكون مخفياً) يخزن المتغيرات الحساسة مثل مفاتيح الاتصال بـ Supabase. **لا يُرفع على GitHub**.
*   **`tsconfig.json`**: قوانين لغة TypeScript لضبط صرامة الكود.
*   **`index.html`**: نقطة دخول المتصفح. الصفحة الوحيدة التي يتم تحميلها فعلياً، ويقوم React بحقن المحتوى داخلها.

### ب) مجلد الكود المصدري `src` (Source)
هنا يوجد "اللحم" والمحتوى الحقيقي للموقع.

#### 1. ملفات الإطلاق
*   **`main.tsx`**: نقطة البداية البرمجية. يربط React بملف `index.html`.
*   **`App.tsx`**: يحتوي على **نظام التوجيه (Router)**، أي الخريطة التي تربط الرابط (URL) بالصفحة المعروضة.
*   **`index.css`**: ملف التنسيق العام (Global Styles) وتعريفات Tailwind.

#### 2. `src/pages` (الصفحات)
كل ملف هنا يمثل صفحة كاملة:
*   **`Home.tsx`**: الصفحة الرئيسية.
*   **`Videos.tsx`**: صفحة الفيديوهات.
*   **`Projects.tsx`**: صفحة المشاريع.
*   **`ProjectDetail.tsx`**: صفحة تفاصيل مشروع معين.
*   **`About.tsx`**: صفحة من نحن.
*   **`Auth/`**: مجلد صفحات تسجيل الدخول وإنشاء الحساب.
*   **`admin/`**: مجلد لوحة التحكم (Dashboard, ArticleManager, etc).

#### 3. `src/components` (المكونات)
قطع "الليغو" الصغيرة التي نستخدمها لبناء الصفحات:
*   **`layout/`**: المكونات الهيكلية الثابتة (Header الشريط العلوي، Footer التذييل).
*   **`common/`**: عناصر عامة يعاد استخدامها (Button, Input, Card).
*   **`home/`, `about/`**: مكونات خاصة بأقسام معينة.

#### 4. `src/services` (الخدمات - API)
طبقة الاتصال بقاعدة البيانات (Supabase):
*   **`supabase.ts`**: إعداد الاتصال الرئيسي.
*   **`auth.ts`**: دوال المصادقة (دخول/خروج).
*   **`articles.ts`**: دوال التعامل مع المقالات (جلب، إضافة، تعديل).
*   **`videos.ts`, `projects.ts`**: دوال الفيديوهات والمشاريع.
*   *الفائدة*: فصل كود البيانات عن كود التصميم.

#### 5. `src/context` (إدارة الحالة)
*   **`AuthContext.tsx`**: يحفظ حالة المستخدم (هل هو مسجل دخول؟ من هو؟) ليكون متاحاً لكل الصفحات.
*   **`ThemeContext.tsx`**: يحفظ وضع الألوان (ليلي/نهاري).

#### 6. `src/i18n` (الترجمة)
*   يحتوي `config.ts` لتهيئة نظام الترجمة.

### ج) مجلد `public`
الملفات العامة التي يمكن الوصول إليها بالرابط المباشر.
*   **`locales/`**: هنا توجد نصوص الموقع بجميع اللغات.
    *   `ar/translation.json`: النصوص العربية.
    *   `en/translation.json`: النصوص الإنجليزية.

### د) مجلد `supabase`
*   **`migrations/`**: ملفات SQL تمثل تاريخ تعديلات قاعدة البيانات. كل ملف يمثل تغييراً (إنشاء جدول، تعديل عمود).

---

## 3. سيناريوهات التعديل: كيف أقوم بـ...؟

### أ. أريد تغيير نص ظاهر في الموقع
1.  لا تعدل ملفات `.tsx` مباشرة إذا كان النص يتغير بتغير اللغة.
2.  اذهب إلى `public/locales/ar/translation.json`.
3.  ابحث عن الجملة وعدلها.

### ب. أريد تغيير لون عام أو نوع الخط
*   اذهب إلى `src/index.css` للتعديلات العامة.
*   أو استخدم كلاسات Tailwind (مثل `text-red-500`) داخل المكونات مباشرة.

### ج. أريد إضافة صفحة جديدة
1.  أنشئ ملفاً جديداً في `src/pages/NewPage.tsx`.
2.  صمم محتوى الصفحة (Component).
3.  افتح `src/App.tsx` وأضف `<Route path="/new-page" element={<NewPage />} />`.

### د. أريد تعديل طريقة جلب البيانات
*   اذهب لمجلد `src/services` وافتح الملف المناسب (مثلاً `articles.ts` للمقالات) وعدل دالة الاستعلام.

### هـ. أريد إضافة عمود جديد في قاعدة البيانات
1.  قم بالتعديل أولاً في Supabase Dashboard.
2.  قم بتوليد ملف Migration جديد وحفظه في `supabase/migrations` لضمان وجود نسخة احتياطية من التعديل.

---

## 4. العمليات الحساسة والأمان (RPCs)
لضمان أمان البيانات، نستخدم **Stored Procedures** (دوال مخزنة) في قاعدة البيانات للعمليات الحساسة.

### أ) الحذف الناعم (Soft Delete) للمقالات
*   **المشكلة**: عند حذف مقال بواسطة الأدمن، لا نريد حذفه نهائياً فوراً، بل إخفاءه.
*   **الحل**: دالة `admin_soft_delete_article` في قاعدة البيانات.
*   **مكان الكود**:
    *   SQL: `supabase/migrations/20260128_admin_soft_delete_rpc.sql`
    *   Service: `src/services/articles.ts` -> `adminDeleteArticle`

### ب) حذف حساب المستخدم
*   **المشكلة**: السماح للمستخدم بحذف حسابه وبياناته بالكامل.
*   **الحل**: دالة `delete_own_account` التي تستخدم `SECURITY DEFINER` لتجاوز قيود الصلاحيات وحذف المستخدم من جدول `auth.users` والبروفايل المرتبط به.
*   **مكان الكود**:
    *   SQL: `supabase/migrations/20260128_delete_account_rpc.sql`
    *   Service: `src/services/profile.ts` -> `deleteUserAccount`
